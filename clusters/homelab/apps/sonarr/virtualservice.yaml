apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: sonarr-tailnet
  namespace: media
  annotations:
    homelab.rst.io/public-funnel: "false"
spec:
  hosts:
    - sonarr.apps.tailnet.example
  gateways:
    - istio-system/tailnet-gateway
  http:
    - route:
        - destination:
            host: sonarr.media.svc.cluster.local
            port:
              number: 8989


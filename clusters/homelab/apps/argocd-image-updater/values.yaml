# Keep this until the Terragrunt-owned Application has switched to the
# marker-only retirement source. Existing live Applications still read this
# file directly from main, so a merge stops the controller even if that apply
# is delayed.
crds:
  install: true
  keep: true

replicaCount: 0

apiVersion: v1
kind: Service
metadata:
  name: media-postgres
  namespace: media
  labels:
    app.kubernetes.io/name: media-postgres
    app.kubernetes.io/part-of: homelab-media
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: media-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres

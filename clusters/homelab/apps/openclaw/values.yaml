serviceAccount:
  openclaw: {}
controllers:
  openclaw:
    serviceAccount:
      identifier: openclaw
    pod:
      automountServiceAccountToken: false
      annotations:
        homelab.rst.io/openclaw-discord-bot-token-ssm-version: "2"
        homelab.rst.io/openclaw-github-app-credentials-ssm-version: "1"
    initContainers:
      bootstrap-config:
        image:
          repository: ghcr.io/openclaw/openclaw
          tag: "2026.5.22@sha256:dcfd148777401d1bbdc63eab5c2f280bbfa912dfb1818566f9d66bb96ffb3f95"
        resources:
          requests:
            cpu: 500m
            memory: 1Gi
          limits:
            memory: 3Gi
        env:
          OPENCLAW_CONFIG_PATH: /data/openclaw/config/openclaw.json
          OPENCLAW_STATE_DIR: /data/openclaw
          GITHUB_APP_PRIVATE_KEY_PATH: /var/run/secrets/openclaw/github-app/private-key.pem
        envFrom:
          - secretRef:
              name: openclaw-secrets
        command:
          - /bin/sh
          - -ec
          - |
            mkdir -p "$(dirname "$OPENCLAW_CONFIG_PATH")"
            if [ ! -s "$OPENCLAW_CONFIG_PATH" ]; then
              printf '{}\n' > "$OPENCLAW_CONFIG_PATH"
            fi

            if ! openclaw config validate; then
              openclaw doctor --fix --non-interactive
            fi

            openclaw config set --strict-json \
              gateway.mode \
              '"local"'

            openclaw config set --strict-json \
              gateway.controlUi.allowedOrigins \
              '["https://openclaw.stinkyboi.com"]'

            openclaw config set --strict-json \
              plugins.entries.codex.enabled \
              'true'

            openclaw config set --strict-json \
              plugins.entries.memory-wiki.enabled \
              'true'

            openclaw config set --strict-json \
              agents.defaults.model \
              '{"primary":"openai/gpt-5.5"}'

            if [ -z "${DISCORD_BOT_TOKEN:-}" ] || [ "$DISCORD_BOT_TOKEN" = "REPLACE_ME" ]; then
              echo "DISCORD_BOT_TOKEN is not populated; skipping Discord channel bootstrap"
            else
              echo "DISCORD_BOT_TOKEN is populated; installing and enabling the Discord channel plugin"
              if ! openclaw plugins info discord >/dev/null 2>&1; then
                openclaw plugins install clawhub:@openclaw/discord
              fi
              openclaw plugins enable discord
              openclaw config set --strict-json \
                channels.discord.enabled \
                'true'
              openclaw config set \
                channels.discord.token \
                --ref-provider default \
                --ref-source env \
                --ref-id DISCORD_BOT_TOKEN
            fi
    containers:
      app:
        image:
          repository: ghcr.io/openclaw/openclaw
          tag: "2026.5.22@sha256:dcfd148777401d1bbdc63eab5c2f280bbfa912dfb1818566f9d66bb96ffb3f95"
        env:
          OPENCLAW_CONFIG_PATH: /data/openclaw/config/openclaw.json
          OPENCLAW_STATE_DIR: /data/openclaw
          GITHUB_APP_PRIVATE_KEY_PATH: /var/run/secrets/openclaw/github-app/private-key.pem
          LITELLM_BASE_URL: http://litellm.ai.svc.cluster.local:4000
        envFrom:
          - secretRef:
              name: openclaw-secrets
        resources:
          requests:
            cpu: 1
            memory: 2Gi
          limits:
            memory: 6Gi
      proxy:
        image:
          repository: ghcr.io/openclaw/openclaw
          tag: "2026.5.22@sha256:dcfd148777401d1bbdc63eab5c2f280bbfa912dfb1818566f9d66bb96ffb3f95"
        command:
          - python3
          - -u
          - -c
          - |
            import asyncio
            import contextlib

            LISTEN_HOST = "0.0.0.0"
            LISTEN_PORT = 8080
            TARGET_HOST = "127.0.0.1"
            TARGET_PORT = 18789

            async def pipe(reader, writer):
                try:
                    while data := await reader.read(65536):
                        writer.write(data)
                        await writer.drain()
                except (BrokenPipeError, ConnectionError):
                    pass
                finally:
                    with contextlib.suppress(Exception):
                        writer.write_eof()
                    writer.close()
                    with contextlib.suppress(Exception):
                        await writer.wait_closed()

            async def handle(client_reader, client_writer):
                peer = client_writer.get_extra_info("peername")
                try:
                    upstream_reader, upstream_writer = await asyncio.open_connection(
                        TARGET_HOST,
                        TARGET_PORT,
                    )
                except OSError as exc:
                    print(f"failed to connect upstream for {peer}: {exc}", flush=True)
                    client_writer.close()
                    with contextlib.suppress(Exception):
                        await client_writer.wait_closed()
                    return

                await asyncio.gather(
                    pipe(client_reader, upstream_writer),
                    pipe(upstream_reader, client_writer),
                    return_exceptions=True,
                )

            async def main():
                server = await asyncio.start_server(
                    handle,
                    LISTEN_HOST,
                    LISTEN_PORT,
                    backlog=256,
                )
                print(
                    f"proxying {LISTEN_HOST}:{LISTEN_PORT} to {TARGET_HOST}:{TARGET_PORT}",
                    flush=True,
                )
                async with server:
                    await server.serve_forever()

            asyncio.run(main())
        probes:
          readiness:
            enabled: true
            custom: true
            spec:
              exec:
                command:
                  - python3
                  - -c
                  - |
                    import socket
                    with socket.create_connection(("127.0.0.1", 18789), timeout=2):
                        pass
              failureThreshold: 3
              periodSeconds: 5
              timeoutSeconds: 3
        resources:
          requests:
            cpu: 25m
            memory: 64Mi
          limits:
            memory: 256Mi
service:
  app:
    controller: openclaw
    ports:
      http:
        port: 8080
persistence:
  config:
    enabled: true
    storageClass: nfs-default
    size: 10Gi
    accessMode: ReadWriteOnce
    advancedMounts:
      openclaw:
        app:
          - path: /data
        bootstrap-config:
          - path: /data
        proxy:
          - path: /data
  plugin-cache:
    enabled: true
    type: emptyDir
    advancedMounts:
      openclaw:
        app:
          - path: /data/openclaw/npm
        bootstrap-config:
          - path: /data/openclaw/npm
  plugin-extensions:
    enabled: true
    type: emptyDir
    advancedMounts:
      openclaw:
        app:
          - path: /data/openclaw/extensions
        bootstrap-config:
          - path: /data/openclaw/extensions
  github-app-private-key:
    enabled: true
    type: secret
    name: openclaw-github-app-private-key
    items:
      - key: private-key.pem
        path: private-key.pem
    advancedMounts:
      openclaw:
        app:
          - path: /var/run/secrets/openclaw/github-app
            readOnly: true
        bootstrap-config:
          - path: /var/run/secrets/openclaw/github-app
            readOnly: true

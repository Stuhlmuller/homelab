controllers:
  tines:
    replicas: 0
    containers:
      app:
        image:
          repository: ghcr.io/tines/self-hosted
          # Private licensed image tag must be pinned before scaling above zero.
          tag: pinned-version-required
        envFrom:
          - secretRef:
              name: tines-secrets
service:
  app:
    controller: tines
    ports:
      http:
        port: 8080
persistence:
  data:
    enabled: true
    storageClass: nfs-default
    size: 20Gi
    accessMode: ReadWriteOnce
    globalMounts:
      - path: /data

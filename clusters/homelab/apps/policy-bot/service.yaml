apiVersion: v1
kind: Service
metadata:
  name: policy-bot
  namespace: automation
  labels:
    app.kubernetes.io/name: policy-bot
    app.kubernetes.io/part-of: homelab-automation
spec:
  selector:
    app.kubernetes.io/name: policy-bot
  ports:
    - name: http
      port: 8080
      targetPort: http

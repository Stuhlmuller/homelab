apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - externalsecret.yaml
  - postgres.yaml
  - authorizationpolicy.yaml
  - virtualservice.yaml
  - runtime-storage.yaml
  - runtime.yaml
configMapGenerator:
  - name: multica-runtime-config
    namespace: ai
    files:
      - runtime/bootstrap.py
      - runtime/settings.json
      - runtime/codex.toml

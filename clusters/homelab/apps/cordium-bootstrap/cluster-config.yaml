---
apiVersion: cordium/v1
kind: ClusterConfig
metadata:
  name: default
spec:
  space:
    ownership:
      rules:
        - condition:
            matchAny: true
          effect: ALLOW
  workspace:
    limit:
      maxPerUser: 4
      maxActivePerUser: 1
    storage:
      storageClass:
        rules:
          - condition:
              matchAny: true
            storageClass: cordium-local

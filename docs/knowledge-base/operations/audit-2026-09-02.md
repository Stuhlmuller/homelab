# Homelab Audit — 2026-09-02

<!-- markdownlint-configure-file { "MD013": { "tables": false } } -->

Tags: #operations #audit #security #reliability

Initial audit baseline: `08cdf5b7dc16bf00cacec54d66695f19679e1786`.
Before rollout, the fixes were rebased onto then-current `origin/main`
`e833c3707987c2f5021843d336260cf6485ca403`; the affected live Applications
also reported that revision. Repository, rendered desired state, CI policy,
and read-only Kubernetes health were inspected. The audit inspection itself
made no live resource, credential, router, NAS, or hardware change.

## Fixed In Repository

| Finding | Change |
| --- | --- |
| Cloudflare HTTP-200 error bodies produced false DNS success | Both Octelium DNS reconcilers now reject transport, malformed, missing-success, and `success:false` responses; a mocked regression covers both helpers without exposing tokens |
| Future release jobs inherited broad read permission | Workflow default is now deny-all; each current job retains its explicit least-privilege scope |
| Redundant GitHub concurrency syntax required an Actionlint suppression | Removed default `queue: single`, its stale exact assertion, and the one-off linter suppression |
| Control-plane onboarding applied Talos config before validation | Added strict metal validation immediately before the maintenance-mode apply |
| 35 Terragrunt-owned Applications declared normalized-away empty Kustomize options | Removed the no-op objects and reject their return in the static gate |
| Six retired Deluge split-profile secrets remained readable | Kept them as state tombstones but removed External Secrets reader IAM access |
| One explicit Terragrunt unit had no provider lock | Added the Kubernetes provider lock and require a lock for every explicit unit |
| Grafana and OctoBot mounted unused Kubernetes API credentials | Disabled service-account token automount and added Helm-values assertions |
| Media backup depended on a historical ordinal PVC | GitOps now declares the matching retained `data-media-postgres-0` claim with pruning and Application-deletion disabled |
| OpenClaw liveness killed recoverable work repeatedly | Kept fast readiness but extended liveness to a six-minute bound pending rollout and observation |

## Current Live Findings

| Status | Evidence | Required next step |
| --- | --- | --- |
| Recovery open | `zimaboard-2` remains NotReady since August 26; Istio is Degraded and Multus Progressing | Restore authenticated Talos access or physical access, then use the declared recovery runbook |
| Mitigation staged | The pre-rollout recheck found 288 OpenClaw app restarts and 184 liveness kills; all three HTTP health paths stalled together while CPU and memory remained below limits | Roll out through GitOps and require 24 hours without another restart; profile the gateway/session path if it exhausts the six-minute window |
| External authority blocked | The public API UPnP Job still finds no Internet Gateway Device; last success was August 6 | Restore reviewed router mapping authority, then rerun the public end-to-end gate |
| Upstream/runtime diagnosis open | `octelium-gwagent` is Ready and light on resources, but has 11 restarts and 61 one-second liveness failures; latest restart was September 2 | Correlate node/network evidence and the existing Octelium upgrade work before changing generated probes |
| Accepted legacy state | One active Cordium Workspace and five inactive claims remain on `nfs-default`; only new or recreated disposable Workspaces use `cordium-local` | Recreate only disposable Workspaces; do not migrate or delete user data implicitly |
| Rotation design open | Grafana and Octelium Entra passwords expire after one year and unchanged applies cannot rotate them | Coordinate an explicit revision, Grafana `OnChange` refresh, and Octelium native-secret sync before expiry |

All observed PVCs were Bound, all 24 ExternalSecrets were Ready, and the latest
five declared backup CronJobs had succeeded on September 2. Those facts do not
prove independent restore coverage. Prometheus/NFS risk and off-NAS restore
proof remain open in [[../architecture/storage-and-state]]. Rendered-image
inventory and pinning remain under
[issue #791](https://github.com/Stuhlmuller/homelab/issues/791) and draft
[PR #930](https://github.com/Stuhlmuller/homelab/pull/930); this audit did not
duplicate or bypass that work.

## Validation

- Full static gate passed, including all generated Terragrunt units, 38
  Kustomize overlays, IAM policy regression, Checkov, secret scans, and the new
  Cloudflare response contract.
- Conftest passed 75 policy-unit, 616 workflow, and 17,808 rendered Kubernetes
  checks.
- Exact cached Helm renders for Grafana, OctoBot, and OpenClaw passed their
  token/probe contracts and Conftest checks. A server-side media diff accepted
  the retained PVC and changed only its retention/tracking annotations.
- Terragrunt/OpenTofu formatting, Terragrunt HCL validation, Actionlint,
  ShellCheck warning-level checks, Bash/Python syntax, JSON parsing, flake
  evaluation, and whitespace checks passed.
- Private Talos machine configs, provider-backed Azure/SSM plans, image digest
  resolution, external router changes, backup restores, and live rollout were
  unavailable or outside this repository-only repair.

See [[continuous-improvement]], [[validation-gates]],
[[../architecture/gitops-flow]], [[../architecture/secrets-and-identity]], and
[[../architecture/storage-and-state]].

# Homelab Audit — 2026-08-30

<!-- markdownlint-configure-file { "MD013": { "tables": false } } -->

Tags: #operations #audit #security #reliability

Baseline: `b408a858877e3a53c5bbe9da93a927ae6dfabc0e`, also the observed
remote `main`. This audit covers repository source, static gates, Kubernetes
health, and the scoped live repairs below. It does not certify every historical
issue as resolved.
GitHub had 93 open issues and 48 open PRs during inspection; existing fixes were
not duplicated or merged. No secret values or hardware were changed.
Follow-up plans, review fixes, and protected rollout results are tracked in
[PR #934](https://github.com/Stuhlmuller/homelab/pull/934).

## Fixed In Repository

| Finding | Change | Evidence |
| --- | --- | --- |
| Deleted-unit discovery includes catalog templates and operator state | Limit retirement to deployed `IaC/bootstrap` and `IaC/live` units, including explicit-stack removals | `scripts/ci/terragrunt-filter-base.sh`; regression rejects the former behavior |
| Retired-unit destroy plans bypass policy | Run existing Conftest rules on the saved destroy plan before apply | `scripts/ci/terragrunt-apply.sh`; sensitive-deletion regression |
| Synthetic retirement units lack Kubernetes/Helm connections | Reuse the root kubeconfig and shared Kubernetes provider | `scripts/ci/terragrunt-filter-base.sh`; rendered provider assertions |
| Backend-free validation can auto-initialize S3 | Disable Terragrunt auto-init for validation and offline policy tests after explicit backend-free init | `scripts/ci/static-checks.sh`; original validation attempted expired AWS authentication, corrected command stays offline |
| Retired runner and Cloudflare credentials remain readable by ESO | Set `reader_access = false`; retain parameter/state ownership | `IaC/.catalog/units/live/aws-ssm-parameters/terragrunt.hcl`; all 51 runtime references remain covered |
| Session-credential deny also blocks SSM-to-KMS decryption | Exclude AWS forward access sessions from the existing deny while retaining direct-session denial and existing Allow limits | `IaC/modules/aws-github-actions-role-policy/main.tf`; Cordium Warning event identifies explicit KMS denial by the boundary |
| Multica PostgreSQL can restart during slow recovery and report ready without usable SQL | Add recovery-aware startup/liveness, `SELECT 1` readiness, and 120-second shutdown grace | `clusters/homelab/apps/multica/postgres.yaml`; absent-database check proved `pg_isready` succeeds where SQL fails |
| n8n rollout hint recommends changing its encryption key | Preserve the instance key on its PVC | `IaC/terragrunt.stack.hcl`; existing n8n bootstrap contract |
| Recovery docs permit live edits followed by backfilling | Require validated repository-owned recovery before mutation | `ONBOARDING.md`, Argo bootstrap/rollback and Talos maintenance runbooks |

The operator IAM repair, worker containment, and reader-policy cleanup were
applied through reviewed saved Terragrunt plans after the user authorized live
repair. Argo CD rolled out Multica's probes, and the protected targeted n8n
workflow applied its hint correction. Cordium source normalization awaits
[PR #935](https://github.com/Stuhlmuller/homelab/pull/935).
Deleted-unit retirement also needs the
encrypted-state recovery work already proposed in
[PR #837](https://github.com/Stuhlmuller/homelab/pull/837); do not treat the new
policy check as proof that every historical state format can be retired.

## Current Live Findings

| Status | Observation | Required next step |
| --- | --- | --- |
| Contained; recovery open | Three nodes Ready; `zimaboard-2` heartbeat stopped August 26. Terragrunt removed its dataplane label; all bound Octelium Pods are terminating and none of its 73 Pods mounts a PVC | Restore current Talos authentication or identify the worker physically, then follow the degraded recovery runbook; do not force-delete Pods |
| Open: mesh/capacity | Istio and Multus node agents are only 3/4 Ready; stale terminating Octelium Pods remain on the unreachable worker. `zimaboard-0` used 80% memory in the snapshot | Recover worker and validate measured capacity before removing emergency Deployments |
| Secret decryption repaired; source drift pending | Operator boundary applied; `cordium-agent-auth` refreshed at `2026-08-30T16:56:48Z` and became Ready without a forced refresh | Merge the reviewed source-normalization fix; historical failed child hooks remain a separate gate |
| Open: full apply prerequisite | Full dispatch `33325196504` failed; all three Azure provider credentials are absent from `homelab-production` and repository fallback settings. The changed shared root makes Azure credentials mandatory in preflight | Provision the documented Azure CI credential contract and rerun a full exact-main dispatch; do not weaken the gate or advance the full-apply checkpoint |
| Open: public API transport | UPnP last succeeded `2026-08-06T08:50:09Z`, despite ongoing scheduled Jobs | Resolve router authority/mapping, then pass public gRPC and authenticated access checks |
| Partially verified: storage | All observed PVCs Bound; media and Octelium PostgreSQL backup CronJobs have August 30 successes | Prove independent restore; successful Jobs do not establish off-NAS coverage or integrity |
| Open: restart history | AFFiNE, OpenClaw, monitoring, Descheduler and Dispatcharr have accumulated restarts | Correlate timestamps and current rates before attributing an active crash loop; existing fixes and hardware findings remain applicable |

Read-only evidence used `kubectl get nodes`, selected node conditions,
`kubectl top nodes`, unhealthy Pod status, Argo Application status, PVC binding,
CronJob success times, ExternalSecret conditions, and Talos client expiry.
No secret payloads are included here.

Cordium is the only periodic ExternalSecret; the other 23 use `OnChange`.
Their retained Ready conditions do not rule out the same decryption failure
on the next config update or rotation. Cordium's successful scheduled refresh
proves the repaired SSM-to-KMS path; it does not verify every secret consumer or
replace the credential-maintenance gates.

The first two full-unit saved plans passed 56 Conftest checks each and independent
review. The operator plan changed only the existing boundary's session-deny
condition. The node-label plan removed only the Terragrunt-owned dataplane
label from `zimaboard-2`. Each apply reported zero additions, one update, and
zero destructions; state and plan artifacts stayed outside the repository.
AWS's live policy simulator also confirmed direct temporary SSM/KMS requests
remain denied while forwarded KMS decryption succeeds. A separate SSM-unit
plan passed 56 policy checks: three existing reader-policy updates remove only
the two retired token permissions; all secret values, keys, and identities
remain unchanged. The matching OpenTofu 1.11.8 plan was applied separately
through that unit's normal state: zero additions, three updates, zero deletions.
All three live policy documents match the reviewed plan. Cordium's subsequent
scheduled refresh succeeded at `2026-08-30T17:41:48Z`.

Multica is Healthy/Synced at the merged repair revision, with its 120-second
shutdown grace, recovery startup probe, and SQL readiness probe live.
The protected [targeted n8n apply](https://github.com/Stuhlmuller/homelab/actions/runs/33325690054)
succeeded; n8n is Healthy/Synced and its live hint preserves the existing key.
The [failed full apply](https://github.com/Stuhlmuller/homelab/actions/runs/33325196504)
does not count as a successful full reconciliation. Its shared-root diff only
removed implicit plan-output arguments, but the existing conservative Azure
gate still requires the credentials described in
[CI configuration](../../ci-cd.md#github-configuration).

The worker remains NotReady. Its available Talos client certificate is both
expired and untrusted by the current cluster CA; VPN reachability alone cannot
authorize a reboot. See the scoped recovery procedure in
[Talos maintenance](../../talos-control-plane-maintenance.md#degraded-recovery-zimaboard-2).

## Existing Remediation Queue

These are existing issue/PR scopes, not newly verified fixes. Review and
validate their current heads before rollout; storage, privilege, and access
changes must not be merged as an unreviewed batch.

| Area | Existing work and remaining gate |
| --- | --- |
| Worker, hardware, capacity | [#775](https://github.com/Stuhlmuller/homelab/issues/775), [#785](https://github.com/Stuhlmuller/homelab/issues/785), [#793](https://github.com/Stuhlmuller/homelab/issues/793), [#802](https://github.com/Stuhlmuller/homelab/issues/802): recovery access, integrity diagnosis, measured capacity |
| Access and application privilege | [#776](https://github.com/Stuhlmuller/homelab/issues/776), [#777](https://github.com/Stuhlmuller/homelab/issues/777), [#782](https://github.com/Stuhlmuller/homelab/issues/782); [PR #861](https://github.com/Stuhlmuller/homelab/pull/861), [PR #856](https://github.com/Stuhlmuller/homelab/pull/856): credential containment and verified deny paths |
| Organization and CI authority | [#813](https://github.com/Stuhlmuller/homelab/issues/813), [#815](https://github.com/Stuhlmuller/homelab/issues/815), [#859](https://github.com/Stuhlmuller/homelab/issues/859), [#860](https://github.com/Stuhlmuller/homelab/issues/860); [PR #885](https://github.com/Stuhlmuller/homelab/pull/885): reviewed plans, organization authority, read-only identities |
| CI safety | [PR #865](https://github.com/Stuhlmuller/homelab/pull/865), [PR #835](https://github.com/Stuhlmuller/homelab/pull/835), [PR #880](https://github.com/Stuhlmuller/homelab/pull/880), [PR #878](https://github.com/Stuhlmuller/homelab/pull/878): private caches/logs, credential lifetime, event-key policy |
| Credential lifecycle | [#803](https://github.com/Stuhlmuller/homelab/issues/803), [PR #871](https://github.com/Stuhlmuller/homelab/pull/871), [PR #854](https://github.com/Stuhlmuller/homelab/pull/854): rotation, old state versions, upstream kubeconfig validation |
| Argo ownership and readiness | [PR #907](https://github.com/Stuhlmuller/homelab/pull/907), [#889](https://github.com/Stuhlmuller/homelab/issues/889), [#890](https://github.com/Stuhlmuller/homelab/issues/890), [#908](https://github.com/Stuhlmuller/homelab/issues/908), [PR #831](https://github.com/Stuhlmuller/homelab/pull/831): ownership transfer, bounded child sync, hook cleanup authentication |
| Dependency discovery and releases | [PR #910](https://github.com/Stuhlmuller/homelab/pull/910), [PR #911](https://github.com/Stuhlmuller/homelab/pull/911), [PR #884](https://github.com/Stuhlmuller/homelab/pull/884), [PR #883](https://github.com/Stuhlmuller/homelab/pull/883), [PR #881](https://github.com/Stuhlmuller/homelab/pull/881): Nix/Helm/Actions discovery and pinned, serialized releases |
| Vulnerable runtime images | [#789](https://github.com/Stuhlmuller/homelab/issues/789), [#791](https://github.com/Stuhlmuller/homelab/issues/791), [PR #930](https://github.com/Stuhlmuller/homelab/pull/930): rendered-image coverage, actionable scans, compatible upgrades and rollback evidence |
| Storage migrations and backups | [#790](https://github.com/Stuhlmuller/homelab/issues/790), [#792](https://github.com/Stuhlmuller/homelab/issues/792), [#867](https://github.com/Stuhlmuller/homelab/issues/867), [#892](https://github.com/Stuhlmuller/homelab/issues/892), [#894](https://github.com/Stuhlmuller/homelab/issues/894): backup, fencing, destination capacity, restore proof before cutover |
| Runtime isolation | [#784](https://github.com/Stuhlmuller/homelab/issues/784), [PR #829](https://github.com/Stuhlmuller/homelab/pull/829), [PR #823](https://github.com/Stuhlmuller/homelab/pull/823), [PR #849](https://github.com/Stuhlmuller/homelab/pull/849): enforced network policy, namespace separation, least privilege |
| Monitoring and routine behavior | [PR #914](https://github.com/Stuhlmuller/homelab/pull/914), [PR #825](https://github.com/Stuhlmuller/homelab/pull/825), [PR #906](https://github.com/Stuhlmuller/homelab/pull/906), [PR #927](https://github.com/Stuhlmuller/homelab/pull/927): filesystem/runtime health, Descheduler cadence, alerting docs |
| External authority | [#787](https://github.com/Stuhlmuller/homelab/issues/787), [#800](https://github.com/Stuhlmuller/homelab/issues/800), [#801](https://github.com/Stuhlmuller/homelab/issues/801), [#868](https://github.com/Stuhlmuller/homelab/issues/868): organization policy, certificate subscription, router/switch/NAS management |

The full evolving backlog remains in [GitHub issues](https://github.com/Stuhlmuller/homelab/issues)
and [open PRs](https://github.com/Stuhlmuller/homelab/pulls).

## Validation

- All 38 Kustomize overlays rendered.
- Conftest rendered-source gate: 17,696 checks passed; policy unit tests passed.
- IaC formatting and backend-disabled SSM module initialization/validation passed.
- Multica SQL check returned `1`; changed probe contract rendered successfully.
- Multica server-side Kustomize dry-run was accepted (diff exit `1`); no
  resources were persisted. Markdown lint passed for all changed notes and
  runbooks, and the edited CI shell example passed syntax validation.
- Full static gate passed, including Checkov and secret scans; ShellCheck,
  OpenTofu formatting, and whitespace checks passed. Independent diff review
  found no actionable regressions.
- Native offline IAM regression: one test passed, covering four request
  contexts and exact KMS permissions; the original deny fails the same test.
- `nix flake check --offline` passed for `aarch64-darwin`; other systems were
  not evaluated.
- The operator IAM, node-label, and reader-policy saved plans were applied and
  verified, as were the GitOps Multica rollout and targeted n8n registration.
  No reboot, state cleanup, credential rotation, or backup restore was
  attempted. Full-stack automation still requires its missing Azure credentials.

See [[continuous-improvement]], [[validation-gates]],
[[architecture/cluster-topology]], and [[architecture/storage-and-state]].

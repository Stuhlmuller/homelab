# Homelab Audit — 2026-09-04

Baseline: `787f1a42c1231ba8c150f76daa53d8fb3262c37a`.

## Live evidence

- Initial Kubernetes access failed with a sandbox denial, then `no route to
  host`. Authenticated `talosctl version` subsequently succeeded; repeated
  Kubernetes reads and `/readyz` passed without mutation. The transient cause
  remains unproven; do not infer a node outage from that client error alone.
- All four nodes were Ready. Authenticated Talos service checks reported
  healthy apid, containerd, CRI, and kubelet on every node. Control-plane etcd
  status reported no errors, a 62 MB database, and matching Raft/applied indexes.
- OpenClaw was the only nonterminal Pod that was not Ready. Its bootstrap
  container had 289 restarts and rejected retired config keys before plugin
  installation. The repository migration fix is described in
  [[../architecture/storage-and-state]]. PR #953 merged as `0b68b9b6`; Argo CD created replacement Pod
  `openclaw-7ff7d999c6-h7nxq`. Runtime recovery is not yet verified.
- Istio CNI and ztunnel were each 4/4 Ready; all three Istio gateway/control
  Deployments were Ready. Failed `octelium-api-upnp` Jobs still report no valid
  IGD; the last successful renewal remains August 6. The public API still
  fails HTTP and gRPC probes. Router authority remains required.
- All 47 PVCs were Bound and all 24 ExternalSecrets Ready. Each of the five
  backup CronJobs had a recent success. This does not establish independent
  restore coverage; the existing storage finding remains open.
- The Cordium nested wildcard still fails public TLS negotiation.
- The OpenClaw server-side dry-run reports existing restricted Pod Security
  warnings: no explicit seccomp profile, privilege-escalation denial, capability
  drop, or non-root requirement. The Nix toolbox needs a separate root-init
  exception; harden the app, bootstrap, and proxy through Helm values after
  startup recovery, validating toolbox and plugin installation independently.
- NOFX `/` and `/api/health` returned HTTP 200 without the expected Octelium
  unauthorized header. Desired catalog state already requires
  `homelab-human-web-access`; the live VirtualService and Cloudflare tunnel
  route match git. A read-only database projection confirmed live
  `Service/nofx.default` had `isAnonymous: true`, no authorization policy, and
  no authorization-header mode. Git now makes the false anonymous setting
  explicit; [[../apps/nofx]] documents focused repository-owned reconciliation.
  The public admin API is unavailable, so live repair remains pending.

## Backup alert coverage

The shared Grafana backup-staleness alert omitted Octelium PostgreSQL. The
expanded selector covers all five declared nightly jobs, preserving the
existing rule UID and 30-hour threshold. Provisioning version 18 loads the
change through the normal Grafana rollout. Exact Prometheus query tests passed
28 stale, fresh, never-succeeded, young, and unrelated-job scenarios; the old
query failed only the two missing Octelium cases. The full static gate and 560
rendered Kubernetes policy checks passed.

## Validation

Current `main` plus the repair passed the complete static gate, Checkov,
Conftest's 17,808 rendered Kubernetes checks, Actionlint, ShellCheck, and
OpenTofu formatting. Exact app-template 4.4.0 rendering passed 280 additional
policy checks and bootstrap shell syntax validation. The end-to-end check
completed with five failures: public API HTTP and gRPC, Cordium wildcard TLS,
and NOFX's two unauthenticated probes. Callback checks passed.
Server-side diff using Argo CD's field manager accepted the rendered repair;
differences were the intended bootstrap change and omitted renderer-only Argo
tracking annotations. No live resources were changed by that check.

The targeted OpenClaw migration passed synthetic preservation, idempotence,
and malformed-input regression checks. Exact OpenClaw 2026.8.2 CLI validation
failed on the legacy fixture and passed on its migrated result; subsequent
repository-owned config commands completed on disposable local state.

See [[continuous-improvement]] and [[validation-gates]].

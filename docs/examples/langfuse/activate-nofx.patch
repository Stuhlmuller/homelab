diff --git a/clusters/homelab/apps/nofx/deployment.yaml b/clusters/homelab/apps/nofx/deployment.yaml
--- a/clusters/homelab/apps/nofx/deployment.yaml
+++ b/clusters/homelab/apps/nofx/deployment.yaml
@@ -26,5 +26,6 @@ spec:
     spec:
       automountServiceAccountToken: false
+      serviceAccountName: nofx
       imagePullSecrets:
         - name: harbor-pull
       securityContext:
@@ -92,15 +93,28 @@ spec:
             readOnlyRootFilesystem: true
           volumeMounts:
             - name: data
               mountPath: /app/data
             - name: tmp
               mountPath: /tmp
+            - name: litellm-routing
+              mountPath: /etc/nofx
+              readOnly: true
+            - name: litellm-token
+              mountPath: /var/run/secrets/nofx/litellm
+              readOnly: true
       volumes:
         - name: data
           persistentVolumeClaim:
             claimName: nofx-data
         - name: tmp
           emptyDir: {}
+        - name: litellm-routing
+          configMap:
+            name: nofx-litellm-routing
+        - name: litellm-token
+          secret:
+            secretName: nofx-litellm
+            defaultMode: 0440
 ---
 apiVersion: apps/v1
 kind: Deployment

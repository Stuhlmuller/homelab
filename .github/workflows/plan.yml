name: "Homelab Plan"

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]
    paths:
      - "nomad/**"
      - "terraform/**"
      - "root.hcl"
      - "mise.toml"
      - "scripts/deploy-live.sh"
      - "scripts/validate-aws-kms.sh"
      - "scripts/validate-aws-ssm.sh"
      - "scripts/ci/**"
      - ".github/actions/setup-infrastructure/**"
      - ".github/workflows/deploy.yml"
      - ".github/workflows/plan.yml"
      - ".github/workflows/validate.yml"

permissions: {}

concurrency:
  group: terragrunt-plan-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  plan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
      pull-requests: write
    env:
      ARTIFACT_NAME: terragrunt-plan-pr-${{ github.event.pull_request.number }}-${{ github.sha }}
      PLAN_LOG: artifacts/terragrunt-plan.log
      PLAN_SUMMARY: artifacts/terragrunt-plan-summary.md
      RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
      TS_AUTH_KEY: ${{ secrets.TS_AUTH_KEY }}
      TS_OAUTH_CLIENT_ID: ${{ secrets.TS_OAUTH_CLIENT_ID }}
      TS_OAUTH_SECRET: ${{ secrets.TS_OAUTH_SECRET }}
      TG_TF_PATH: tofu
      NOMAD_ADDR: http://100.94.104.7:4646
      WORKING_DIR: terraform/live/homelab
    steps:
      - name: Checkout
        uses: actions/checkout@0c366fd6a839edf440554fa01a7085ccba70ac98
        with:
          persist-credentials: false

      - name: Setup mise
        uses: jdx/mise-action@1648a7812b9aeae629881980618f079932869151 # v4

      - name: Initialize plan artifacts
        shell: bash
        run: mkdir -p artifacts

      - name: Skip privileged access on fork pull requests
        if: github.event.pull_request.head.repo.full_name != github.repository
        shell: bash
        run: echo "Skipping AWS credentials, infrastructure access, and the live Terragrunt plan for fork pull requests."

      - name: Configure AWS credentials
        if: github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '')
        uses: aws-actions/configure-aws-credentials@ec61189d14ec14c8efccab744f656cffd0e33f37 # v6
        with:
          aws-region: us-east-1
          role-to-assume: ${{ vars.AWS_ROLE_TO_ASSUME_HOMELAB }}

      - name: Setup infrastructure access
        if: github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '')
        uses: ./.github/actions/setup-infrastructure
        with:
          aws-region: us-east-1
          tailscale-auth-key: ${{ env.TS_AUTH_KEY }}
          tailscale-auth-key-parameter: ${{ vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER }}
          oauth-client-id: ${{ env.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ env.TS_OAUTH_SECRET }}
          connectivity-probe-address: 100.94.104.7
          connectivity-probe-port: "22"

      - name: Run Terragrunt plan
        if: github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '')
        id: terragrunt_plan
        shell: bash
        run: |
          set -euo pipefail
          terragrunt run --all --tf-path "${TG_TF_PATH}" init --working-dir "${WORKING_DIR}" -- -backend=false -input=false
          set +e
          terragrunt run --all --tf-path "${TG_TF_PATH}" plan --working-dir "${WORKING_DIR}" -- -refresh=false -lock=false -input=false -no-color 2>&1 | tee "${PLAN_LOG}"
          exit_code="${PIPESTATUS[0]}"
          set -e
          echo "exit_code=${exit_code}" >> "${GITHUB_OUTPUT}"

      - name: Render plan summary
        if: always() && github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '')
        shell: bash
        env:
          PLAN_EXIT_CODE: ${{ steps.terragrunt_plan.outputs.exit_code || '1' }}
        run: |
          python3 scripts/ci/terragrunt_plan_report.py render \
            --log-file "${PLAN_LOG}" \
            --exit-code "${PLAN_EXIT_CODE}" \
            --working-dir "${WORKING_DIR}" \
            --run-url "${RUN_URL}" \
            --artifact-name "${ARTIFACT_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            --output-file "${PLAN_SUMMARY}"

      - name: Render skipped plan summary when repo configuration is missing
        if: always() && github.event.pull_request.head.repo.full_name == github.repository && (vars.AWS_ROLE_TO_ASSUME_HOMELAB == '' || ((env.TS_OAUTH_CLIENT_ID == '' || env.TS_OAUTH_SECRET == '') && env.TS_AUTH_KEY == '' && vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER == ''))
        shell: bash
        run: |
          python3 scripts/ci/terragrunt_plan_report.py render-static \
            --status skipped \
            --working-dir "${WORKING_DIR}" \
            --run-url "${RUN_URL}" \
            --artifact-name "${ARTIFACT_NAME}" \
            --commit-sha "${GITHUB_SHA}" \
            --summary-line "Terragrunt plan skipped because the repository is missing required GitHub Actions configuration." \
            --summary-line "Configure the \`AWS_ROLE_TO_ASSUME_HOMELAB\` variable plus either the \`TS_OAUTH_CLIENT_ID\` and \`TS_OAUTH_SECRET\` secrets, the \`TS_AUTH_KEY\` secret, or the \`TAILSCALE_AUTH_KEY_SSM_PARAMETER\` variable to enable trusted-branch plans on pull requests." \
            --output-file "${PLAN_SUMMARY}"

      - name: Upload plan artifact
        if: always() && github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '')
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: artifacts/
          if-no-files-found: error

      - name: Update PR description
        if: always() && github.event.pull_request.head.repo.full_name == github.repository
        shell: bash
        env:
          GITHUB_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          python3 scripts/ci/terragrunt_plan_report.py update-pr-body \
            --repo "${GITHUB_REPOSITORY}" \
            --pr-number "${PR_NUMBER}" \
            --summary-file "${PLAN_SUMMARY}"

      - name: Fail when plan fails
        if: github.event.pull_request.head.repo.full_name == github.repository && vars.AWS_ROLE_TO_ASSUME_HOMELAB != '' && ((env.TS_OAUTH_CLIENT_ID != '' && env.TS_OAUTH_SECRET != '') || env.TS_AUTH_KEY != '' || vars.TAILSCALE_AUTH_KEY_SSM_PARAMETER != '') && steps.terragrunt_plan.outputs.exit_code != '0'
        shell: bash
        run: exit 1

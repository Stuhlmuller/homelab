name: "Release"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: Stuhlmuller/workflows/.github/workflows/release.yml@9de6d6310256cd5ff99d29bca00ce4e5201671c0 # main

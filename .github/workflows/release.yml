name: "Release"

on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize, reopened, edited]
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    uses: Stuhlmuller/workflows/.github/workflows/release.yml@62b379041d7aeaaa92f007ab66c48d9f2d2ec392 # main
